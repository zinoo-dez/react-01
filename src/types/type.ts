export interface Category {
    category_id: number;
    name: string;
    description: string;
    created_ad: string;
    updated_at: string;
}
export interface Product {
    product_id: number;
    name: string;
    price: number;
}